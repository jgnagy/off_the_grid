#!/usr/bin/env ruby

require 'off_the_grid'

include OffTheGrid

rendered_template = ARGV[0]
file_to_edit      = ARGV[1]

File.open(file_to_edit, 'w') do |file|
  File.readlines(rendered_template, 'r').each do |line|
    file.puts line
  end
end
